# 后续优先级

> 说明：下面这些是 **第一优先级做完之后** 再继续做的事情。今天先不展开实现，留作后续排期。

---

## 优先级 2：把配置层真正规范化

### 目标
把现在已经有的配置文件，变成真正稳定的“单一事实来源”。

### 要做什么
- 统一 `spider_base`、腿安装位姿、默认站姿的配置语义
- 把 `leg_params.yaml` 继续补全成几何主配置
- 把 `default_pose.yaml` 从占位值收敛成真正的默认姿态
- 把 `servo_map.yaml` 继续补齐方向、偏移、机械镜像相关字段
- 让 `leg_calc` 真正从配置里读，而不是靠代码里的默认理解

### 相关文件
- `src/spider/config/leg_params.yaml`
- `src/spider/config/default_pose.yaml`
- `src/spider/config/servo_map.yaml`
- `src/leg_calc/src/main.cpp`
- `src/leg_calc/include/leg_calc/common_types.hpp`

---

## 优先级 3：把数学核心继续拆纯

### 目标
把 `leg_calc` 做成更稳定的纯软件数学层。

### 要做什么
- 继续把 `FootTrajectory` 做完整
- 给足端轨迹加入更清楚的步长、抬腿高度、转向策略
- 把 `GaitPhaseManager` 的配置和策略整理得更干净
- 让 `leg_calc/src/main.cpp` 继续变薄，只负责装配
- 如果后面需要，再考虑把 IK/FK 再进一步封装成纯库接口

### 相关文件
- `src/leg_calc/include/leg_calc/foot_trajectory.hpp`
- `src/leg_calc/src/foot_trajectory.cpp`
- `src/leg_calc/include/leg_calc/gait_types.hpp`
- `src/leg_calc/include/leg_calc/gait_phase_manager.hpp`
- `src/leg_calc/src/gait_phase_manager.cpp`
- `src/leg_calc/src/main.cpp`
- `src/leg_calc/include/leg_calc/leg_kinematics.hpp`
- `src/leg_calc/src/leg_kinematics.cpp`

---

## 优先级 4：谨慎评估外部 GitHub 运动学库

### 目标
确认是否真的有必要替换或补充当前 KDL + Eigen 路线。

### 原则
- 先保留当前实现
- 只做评估，不急着接入
- 只有在许可、维护、依赖、数值稳定性都合格时才考虑引入

### 评估标准
- 许可证兼容
- ROS2 / colcon 集成成本低
- 不引入难维护的 ABI 依赖
- 确实解决当前缺口
- 不只是重复 KDL/Eigen 已能做的事情
- 数值行为稳定

### 相关文件
- `src/leg_calc/include/leg_calc/leg_kinematics.hpp`
- `src/leg_calc/src/leg_kinematics.cpp`
- `src/leg_calc/src/main.cpp`

---

## 优先级 5：补测试和离线验证

### 目标
让纯软件阶段更稳，不靠真机试错。

### 要做什么
- IK / FK 基础测试
- 坐标系转换测试
- 步态相位测试
- `Servo18` 映射测试
- 命令超时和状态机切换测试
- 离线回放测试

### 相关文件
- `src/leg_calc/src/leg_kinematics.cpp`
- `src/leg_calc/src/gait_phase_manager.cpp`
- `src/leg_calc/src/servo18_mapper.cpp`
- `src/spider_task/src/main.cpp`
- 后续新增测试文件

---

## 优先级 6：做更完整的软件可视化

### 目标
不接硬件，也能看得见系统行为。

### 要做什么
- 足端轨迹可视化
- 关节角曲线输出
- 步态相位显示
- 默认站姿和运动姿态对比

### 相关文件
- `src/leg_calc/src/main.cpp`
- 后续可新增独立工具包或脚本

---

## 优先级 7：驱动层再分清一点

### 目标
让 `robot_driver` 更像真正的驱动层，而不是只会 fake-send 的占位节点。

### 要做什么
- 把组帧和传输再分层
- 保持 fake-send 可用
- 未来真接 USB / 串口时，只替换传输层

### 相关文件
- `src/robot_driver/src/main.cpp`
- `src/robot_driver/CMakeLists.txt`

---

## 当前建议顺序

如果后面继续做，我建议按这个顺序：

1. 配置层规范化
2. 数学核心继续拆纯
3. 再考虑外部库
4. 补测试
5. 做可视化
6. 最后再收紧 driver 层

---

## 一句话总结

第一优先级已经结束，后面最值得继续的就是：**配置规范化 → 数学层继续拆纯 → 测试和可视化 → 最后再评估第三方库。**
