# 蜘蛛机器人上位机工程骨架设计（学习版详细说明）

## 1. 这份文档是干什么的

这份文档不是单纯给你“定一个目录”这么简单。

它有两个目标：

1. **帮你把项目骨架先搭清楚**：知道整个工程应该分成哪些部分。
2. **帮你边做边学**：你不只是照抄目录，而是能理解“为什么要这样分”。

你现在是**大一刚结束**，这是一个很好的阶段：

- 数学、编程、单片机、控制都还在逐步建立
- 这时候最重要的不是“立刻写最复杂的代码”
- 而是先形成一种**工程化思维**

所谓工程化思维，就是：

> 一个机器人项目，不是一个 cpp 文件从头写到尾，而是要拆成不同模块，每个模块只负责自己的事。

这也是你现在做这个蜘蛛机器人上位机最值得学的东西之一。

---

## 2. 先理解：什么叫“上位机”

在你的项目里，大致有两层：

### 2.1 下位机
一般是 STM32 这种单片机。

它更靠近硬件，负责：

- 接收上位机发来的控制命令
- 解析协议
- 驱动舵机
- 读取传感器（如果有）
- 以很高频率、很稳定地执行底层动作

你可以把它理解成：

> 机器人身体里负责“直接动手”的部分。

---

### 2.2 上位机
一般是 Ubuntu + ROS2 跑在电脑或者工控机上。

它更靠近“思考和组织”，负责：

- 决定机器人现在该做什么
- 把动作意图变成具体关节目标
- 做逆运动学、步态、姿态补偿
- 把数据通过串口 / USB 发给下位机
- 显示机器人模型和状态

你可以把它理解成：

> 机器人身体外面负责“出主意、算方案、发指令”的部分。

---

### 2.3 为什么要分上下位机
因为两者擅长的事不一样。

#### 下位机擅长：
- 实时性强
- 直接操作硬件
- 延迟小
- 稳定循环控制

#### 上位机擅长：
- 算法更复杂
- 逻辑更清晰
- 易于调试
- 易于接入 ROS2、RViz、日志、可视化

所以好的机器人系统往往是：

- **上位机负责“想”**
- **下位机负责“做”**

---

## 3. 为什么这个项目不能一开始就写成一个大程序

很多初学者最容易犯的错误是：

- 一个主程序里既写串口
- 又写舵机角度计算
- 又写状态机
- 又写初始化动作
- 又写调试打印

最后会变成一个“能跑，但很乱”的工程。

短期看好像快，长期会非常难维护。

### 例如会出什么问题？

如果你把所有功能都写在一个节点里：

- 串口有 bug，不知道是协议错了还是算法错了
- 舵机不动，不知道是 topic 没发出来还是打包错了
- 后面想加 IMU 补偿，会改一堆旧代码
- 想换协议，结果整个项目都得重写

所以我们要从一开始就学会一件很重要的事：

> **按职责拆模块。**

这比“先把功能堆出来”更重要。

---

## 4. ROS2 工程为什么要分 package

ROS2 里的一个核心概念是：**package（功能包）**。

你可以把 package 理解成：

> 一个功能明确的小工程。

每个包里面可以放：

- 源代码
- 配置文件
- launch 启动文件
- 消息定义
- 模型文件

### 为什么不把所有东西放在一个 package 里？
因为不同东西的变化频率不同、职责也不同。

比如：

- 消息定义是全工程共享的
- 驱动代码只负责通信
- 逆运动学只负责计算
- 启动文件只负责组织节点

所以拆成多个包后：

- 查找方便
- 编译清晰
- 依赖关系明确
- 后续扩展不容易混乱

这也是你现在最该学会的 ROS2 思路之一。

---

## 5. 整个工程的总结构是什么

我们先定一个最终目标结构：

```bash
spider_upper/
└── src/
    ├── spider/
    ├── robot_interfaces/
    ├── robot_driver/
    ├── leg_calc/
    ├── spider_task/
    └── launch_pack/
```

这里要先理解两层：

### 5.1 `spider_upper/`
这是**整个工作区根目录**。

以后你会在这里执行：

- `colcon build`
- `source install/setup.bash`
- `ros2 launch ...`

也就是说，它相当于整个项目的大本营。

---

### 5.2 `src/`
这是 ROS2 约定俗成的源码目录。

所有你自己写的 package 都放这里。

为什么叫 `src`？
因为它就是 source code 的缩写，也就是源码。

---

### 5.3 编译后为什么会多出 `build/ install/ log/`
以后你编译工作区时，ROS2 会自动生成：

```bash
spider_upper/
├── build/
├── install/
├── log/
└── src/
```

#### `build/`
编译过程中的中间文件。

#### `install/`
编译完成后真正被 ROS2 使用的结果。

#### `log/`
编译日志和运行日志。

你可以先记住一句最实用的话：

> `src` 是你写代码的地方，`install` 是 ROS2 真正拿来运行的地方。

---

## 6. 六个 package 分别是干什么的

下面这部分最重要。我会尽量用“新手能理解”的方式讲。

---

## 6.1 `spider` 包：机器人的“档案馆”

### 它的定位
`spider` 这个包不是拿来控制机器人的。

它更像一个：

> **机器人静态资料总库 / 档案馆**

这里面放的东西通常不会频繁变化，也不是“运行中的逻辑”。

### 它里面应该放什么

例如：

- 机器人 URDF / Xacro 模型
- 每条腿的长度参数
- 舵机编号和关节对应关系
- 舵机零位偏移
- 舵机方向正负
- 默认站立姿态
- RViz 要读取的模型资源

### 为什么这些要单独放在这里
因为这些东西属于：

> “机器人本身是什么样子”

而不是：

> “机器人现在想做什么动作”

这两个概念必须分开。

#### 举个例子
比如一条腿有三段：

- coxa
- femur
- tibia

它们的长度是固定的，比如：

- coxa = 35 mm
- femur = 70 mm
- tibia = 100 mm

这些参数不是任务逻辑，也不是通信逻辑。
它们属于**机器人固有属性**，所以就应该放到 `spider/config/`。

### 推荐目录

```bash
spider/
├── config/
│   ├── leg_params.yaml
│   ├── servo_map.yaml
│   ├── servo_offset.yaml
│   └── default_pose.yaml
├── model/
│   ├── spider.urdf.xacro
│   └── materials.xacro
├── meshes/
└── package.xml
```

### 逐个解释这些文件/目录

#### `config/`
用来放配置参数。

##### `leg_params.yaml`
存腿长、关节几何参数。

##### `servo_map.yaml`
存“第几个舵机对应哪条腿的哪个关节”。

比如：
- 0 号舵机 → 左前腿 coxa
- 1 号舵机 → 左前腿 femur
- 2 号舵机 → 左前腿 tibia

这张表特别重要，因为如果映射错了，你发左前腿，可能右后腿在动。

##### `servo_offset.yaml`
每个舵机的零位修正。

真实世界里，舵机装配不可能完美 100% 对齐，所以你一定会遇到：

- 理论上应该 90°
- 实际装上去却偏了 7°

这时不能每次都在代码里硬修，而是应该把修正量放进配置文件。

##### `default_pose.yaml`
默认站立姿态，比如 18 个关节的初始角度。

这样上电后你可以先进入一个安全姿态。

---

#### `model/`
放机器人模型。

##### `spider.urdf.xacro`
这是描述机器人结构最重要的文件之一。

URDF 可以理解成：

> 用文本方式描述一个机器人由哪些连杆、哪些关节连接而成。

Xacro 是 URDF 的增强版，可以写宏、参数，减少重复。

对于六足机器人这种左右很多结构相似的模型，Xacro 很有用。

##### `materials.xacro`
可以把颜色、材质之类单独放开。

---

#### `meshes/`
如果你以后有 3D 模型文件，比如 `.stl`、`.dae`，一般就放这里。

---

### `spider` 包不应该做什么
它不应该：

- 发串口
- 跑步态
- 做状态机
- 处理按键输入

因为它只是“档案馆”，不是“控制中心”。

---

## 6.2 `robot_interfaces` 包：全工程的“公共语言”

### 它的定位
这个包是给所有节点统一说话用的。

你可以把它理解成：

> **全班统一使用的术语表和格式说明书**

在 ROS2 里，不同节点之间要靠消息通信。
比如：

- 任务节点发一个“前进命令”
- 计算节点发一个“18 路舵机角”
- 驱动节点发一个“当前状态”

如果每个人都自己定义格式，整个工程就会很乱。

所以我们把所有公共消息定义集中放在 `robot_interfaces`。

### 推荐目录

```bash
robot_interfaces/
└── msg/
    ├── Servo18.msg
    ├── LegTarget.msg
    ├── SpiderPose.msg
    ├── SpiderState.msg
    └── GaitCommand.msg
```

### 为什么消息定义很重要
因为消息定义实际上就是：

> 你的模块之间如何交流的“协议”。

设计不好，后面改起来会很痛苦。

---

### 详细解释每个消息

#### 1）`Servo18.msg`
这是最接近下位机协议的消息。

```msg
std_msgs/Header header
int16[18] angle_ddeg
uint8 seq
```

##### 作用
表示：

- 一次要发给 18 个舵机的目标角度

##### 为什么它很重要
因为你的蜘蛛有：

- 6 条腿
- 每条腿 3 个关节
- 总共 18 个自由度

很多时候，上位机最终都要落到：

> “这 18 个舵机分别该转到多少角度？”

所以这是一个**底层通用终点消息**。

##### 字段解释
- `header`：时间戳、坐标系等信息，ROS2 常见写法
- `angle_ddeg`：18 个整数角度
- `seq`：可选序号，调试通信很有帮助

##### 为什么角度建议用整数而不是 float
因为和下位机通信时，整数更直接：

- 打包方便
- 不容易受浮点精度影响
- 与 C 语言结构体更好对应

比如 90.0° 可以存成 900（单位 0.1°）。

---

#### 2）`LegTarget.msg`
```msg
string leg_name
float32 coxa_deg
float32 femur_deg
float32 tibia_deg
```

##### 作用
表示单条腿的目标关节角。

##### 为什么需要它
因为调试六足机器人时，常常不是一上来就管 18 路舵机，而是先调：

- 左前腿有没有算对
- 右中腿是不是方向反了
- 某一条腿抬起动作是否正常

这时用“单腿消息”会比一上来塞 18 个角更清楚。

---

#### 3）`SpiderPose.msg`
```msg
std_msgs/Header header
LegTarget lf
LegTarget rf
LegTarget lm
LegTarget rm
LegTarget lr
LegTarget rr
```

##### 作用
表示整机 6 条腿的目标姿态。

##### 为什么不用永远都只发 `Servo18`
因为 `Servo18` 虽然底层方便，但它太“硬件视角”了。

看 `Servo18` 时你只知道：
- 第 0 个舵机多少度
- 第 1 个舵机多少度

但你不容易直观看出：
- 左前腿在干嘛
- 右后腿在干嘛

而 `SpiderPose` 更“机器人视角”。
它更适合：

- 调试运动学
- 人类阅读
- 打印日志
- 中间层传输

---

#### 4）`GaitCommand.msg`
```msg
std_msgs/Header header
string mode
float32 vx
float32 vy
float32 wz
float32 body_height
float32 step_height
float32 step_length
```

##### 作用
表示高层运动命令。

这条消息不是在说“18 个舵机该怎么转”，而是在说：

> “我希望机器人做什么运动。”

##### 字段理解
- `mode`：模式，比如 `stand`、`walk`、`turn`
- `vx`：前进后退速度
- `vy`：左右平移速度（如果以后支持）
- `wz`：绕 z 轴转动速度，也就是转向
- `body_height`：机体高度
- `step_height`：抬腿高度
- `step_length`：步长

##### 为什么它重要
因为这是“任务层”和“算法层”之间最自然的接口。

任务层说：
- 我要前进
- 我要站立

算法层负责理解这个意图，并计算出具体关节角。

---

#### 5）`SpiderState.msg`
```msg
std_msgs/Header header
string mode
bool driver_online
bool servo_online
int16[18] current_angle_ddeg
```

##### 作用
表示当前机器人状态。

##### 为什么需要状态消息
因为一个系统不能只会发命令，还要能知道自己现在什么状态。

比如：
- 驱动在线吗
- 舵机在线吗
- 当前模式是什么
- 当前角度大概是多少

这对：
- 调试
- 监控
- 错误处理
都很重要。

---

### `robot_interfaces` 包不应该做什么
它不应该：

- 写算法
- 写节点逻辑
- 写串口

因为它只是**定义大家说话的格式**。

---

## 6.3 `robot_driver` 包：机器人和 STM32 之间的“翻译官”

### 它的定位
这是非常关键的一个包。

你可以把它理解成：

> **ROS2 世界 和 STM32 世界之间的翻译官**

为什么是翻译官？
因为 ROS2 这边看到的是：

- topic
- msg
- 节点
- 时间戳

而 STM32 那边看到的是：

- 字节流
- 帧头帧尾
- 校验位
- 串口设备

这两边语言不一样，所以必须有一个中间层做“翻译”。

---

### 它应该负责什么

- 打开串口或 USB CDC 设备
- 把 `Servo18.msg` 打包成下位机能懂的字节协议
- 发送给 STM32
- 可选接收 STM32 返回的数据
- 发布当前驱动状态
- 生成 `joint_states` 给 RViz 或其他工具看

---

### 推荐目录

```bash
robot_driver/
├── include/robot_driver/
│   ├── serial_port.hpp
│   ├── protocol.hpp
│   └── driver_node.hpp
├── src/
│   ├── main.cpp
│   ├── driver_node.cpp
│   ├── serial_port.cpp
│   └── protocol.cpp
├── config/
│   └── driver.yaml
└── package.xml
```

### 逐个解释

#### `serial_port.hpp / serial_port.cpp`
负责和系统里的串口设备打交道。

比如：
- `/dev/ttyUSB0`
- `/dev/ttyACM0`

它主要处理：
- 打开设备
- 设置波特率
- 发送字节
- 接收字节
- 关闭设备

你可以把它理解成：

> 专门和“线”打交道的部分。

---

#### `protocol.hpp / protocol.cpp`
负责“协议打包和解包”。

什么意思？
比如你上层有一个：

- 18 路目标角度数组

但 STM32 可能要求你发的是：

- 帧头 2 字节
- 18 路角度共 36 字节
- 序号 1 字节
- 校验 1 字节
- 帧尾 2 字节

这时候就要有一个专门模块把“结构化数据”变成“字节流”。

它做的工作像：

- `pack()`：把消息打包成字节
- `unpack()`：把字节解析回结构

这部分非常值得你认真学，因为它连接了：

- 软件结构设计
- 通信协议
- 嵌入式数据格式

---

#### `driver_node.hpp / driver_node.cpp`
这是 ROS2 节点本体。

它负责：

- 订阅 `/spider/servo_target`
- 收到消息后调用 `protocol` 打包
- 再调用 `serial_port` 发送
- 同时把状态发布出去

你可以理解成：

> `driver_node` 是“总指挥”，`serial_port` 和 `protocol` 是它手下的两个工具人。

这样设计的好处是：

- 如果以后协议改了，只改 `protocol`
- 如果以后串口库改了，只改 `serial_port`
- 节点逻辑本身不用大动

这就是模块化设计的价值。

---

#### `main.cpp`
一般就是 ROS2 程序入口。

它的工作通常很简单：

- 初始化 ROS2
- 创建 `DriverNode`
- spin 起来
- 退出时清理资源

很多初学者会把所有代码都塞 `main.cpp`，这是不好的习惯。

正确做法是：

- `main.cpp` 很薄
- 真正逻辑在 node 类里

---

#### `config/driver.yaml`
存驱动参数，例如：

- 串口设备名
- 波特率
- 超时时间
- 是否开启回传解析

这样以后换设备时不用改代码，只改配置文件。

---

### `robot_driver` 最重要的边界
这个包**绝对不要**顺手把这些也写进去：

- 逆运动学
- 步态规划
- 站立/行走状态机

为什么？
因为这样会导致“翻译官开始替总指挥做决定”，职责就乱了。

---

## 6.4 `leg_calc` 包：机器人的“数学大脑”

### 它的定位
这个包是整个上位机里最“像算法”的部分。

你可以把它理解成：

> **负责把动作意图算成关节角的数学大脑**

比如任务层说：

- 我要前进
- 我要站高一点
- 我要左转

`leg_calc` 要负责把这些话翻译成：

- 每条腿应该放到哪里
- 每个关节应该转多少度

---

### 它应该负责什么

- 单腿逆运动学（IK）
- 机体姿态补偿
- 六条腿整体角度生成
- 将高层命令输出成 `Servo18` 或 `SpiderPose`

---

### 什么是逆运动学（IK）
这是你一定会接触到的概念。

#### 正运动学
已知关节角，求脚尖在哪里。

#### 逆运动学
已知脚尖想放到哪里，求关节角应该是多少。

对六足机器人来说，更常见的是逆运动学。

因为高层通常不会说：
- 1 号舵机转 87°
- 2 号舵机转 113°

而是会说：
- 左前腿脚尖抬高一点
- 这一步往前迈 30 mm

于是就需要 IK 来算关节角。

---

### 推荐目录

```bash
leg_calc/
├── include/leg_calc/
│   ├── leg_ik.hpp
│   ├── body_kinematics.hpp
│   └── spider_calc_node.hpp
├── src/
│   ├── main.cpp
│   ├── leg_ik.cpp
│   ├── body_kinematics.cpp
│   └── spider_calc_node.cpp
├── config/
│   └── calc.yaml
└── package.xml
```

### 逐个解释

#### `leg_ik.hpp / leg_ik.cpp`
只负责单腿逆运动学。

输入可能是：
- 某条腿脚尖相对机体的位置 `(x, y, z)`

输出可能是：
- `coxa_deg`
- `femur_deg`
- `tibia_deg`

把这部分单独拿出来非常重要，因为：

- 它数学含量高
- 经常要单独调试
- 容易出符号、象限、单位问题

单独拆出来后，定位 bug 会方便很多。

---

#### `body_kinematics.hpp / body_kinematics.cpp`
负责机体姿态相关计算。

比如机器人身体：
- 前倾一点
- 左右倾一点
- 整体升高一点

这些变化会影响每条腿脚尖相对机体的位置。

所以在做真正整机控制时，往往不是只算单腿，还要算整个身体姿态变化带来的补偿。

你可以把它理解成：

> “当机身姿势变了，每条腿该怎么跟着补偿”。

---

#### `spider_calc_node.hpp / spider_calc_node.cpp`
这是 ROS2 节点本体。

它订阅上层命令，比如：
- `/spider/cmd_gait`
- `/spider/cmd_pose`

然后：
1. 根据命令决定目标步态/姿态
2. 调用运动学模块计算
3. 输出 `/spider/servo_target`

它是算法模块和 ROS2 通信之间的桥。

---

#### `config/calc.yaml`
这里可以放：

- 默认机体高度
- 抬腿高度
- 步长范围
- 关节角限位
- 安全范围

这样算法调整时可以先改参数，不用每次改源码。

---

### `leg_calc` 不应该做什么
它不应该：

- 自己去开串口
- 自己决定当前模式
- 自己读取键盘手柄

因为它的核心职责只有一个：

> **算。**

---

## 6.5 `spider_task` 包：机器人的“行为指挥中心”

### 它的定位
这个包负责更高层的行为。

你可以把它理解成：

> **机器人现在要做什么，由它决定。**

比如：
- 上电先归中
- 然后站立
- 接到命令后前进
- 出现异常时停下

这些都属于“任务逻辑”。

---

### 它应该负责什么

- 初始化动作
- 站立 / 趴下 / 复位
- 简单步态流程控制
- 模式切换
- 标定流程
- 演示动作

---

### 推荐目录

```bash
spider_task/
├── include/spider_task/
│   ├── state_machine.hpp
│   ├── gait_node.hpp
│   └── calibration_node.hpp
├── src/
│   ├── main.cpp
│   ├── gait_node.cpp
│   ├── state_machine.cpp
│   └── calibration_node.cpp
├── config/
│   └── gait.yaml
└── package.xml
```

### 逐个解释

#### `state_machine.hpp / state_machine.cpp`
这里建议放状态机。

状态机是什么？
简单理解就是：

> 机器人在不同状态下，允许做的事情不一样。

例如：
- `IDLE`：空闲
- `INIT`：初始化
- `STAND`：站立
- `WALK`：行走
- `CALIBRATION`：标定
- `ERROR`：错误状态

为什么要状态机？
因为你不能让机器人：

- 一边初始化一边乱走
- 出错时还继续执行动作
- 标定中又突然切换步态

状态机会让控制逻辑更安全、更清晰。

---

#### `gait_node.cpp`
这里可以理解成行为输出节点。

它可能负责：
- 定时发布高层 gait 命令
- 根据当前模式生成不同动作阶段
- 做一些简单测试动作

注意：
这里的 “gait” 在第一版不一定要很复杂。
它甚至可以先只是：
- 发布站立命令
- 发布单腿抬起测试命令
- 发布简单前进命令

也就是说，一开始先把“任务组织”学明白，比一步到位做高级步态更重要。

---

#### `calibration_node.cpp`
这个很适合单独拆出来。

因为标定经常有专门流程，比如：
- 所有舵机回到中位
- 单独调某条腿
- 保存偏移参数

标定和“正常运行”不是一回事，所以单独一个节点会更干净。

---

#### `config/gait.yaml`
存步态相关参数，比如：
- 默认频率
- 步长
- 抬腿高度
- 初始化等待时间

这样任务行为也能参数化。

---

### `spider_task` 和 `leg_calc` 有什么区别
这两个包很容易混淆。

你可以这样区分：

#### `spider_task`
负责：
- 现在做什么
- 模式切换
- 行为顺序

更像“指挥官”。

#### `leg_calc`
负责：
- 这个动作具体怎么算
- 每条腿角度如何得到

更像“数学工程师”。

一个负责决定目标，一个负责把目标算出来。

---

## 6.6 `launch_pack` 包：项目的“一键启动面板”

### 它的定位
这个包本身通常不写核心算法。

它更像：

> **把多个节点按某种用途组合起来的一键启动面板。**

比如你现在可能有这些节点：
- driver 节点
- calc 节点
- task 节点
- robot_state_publisher
- rviz2

如果每次都手动一个个开，非常麻烦。

所以需要 launch 文件统一组织。

---

### 推荐目录

```bash
launch_pack/
├── launch/
│   ├── spider_real.launch.py
│   ├── spider_display.launch.py
│   ├── spider_calibration.launch.py
│   └── spider_walk_test.launch.py
├── config/
├── rviz/
│   └── spider.rviz
└── package.xml
```

### 逐个解释

#### `launch/`
这里放启动脚本。

在 ROS2 里，launch 文件的作用就是：

- 启动哪些节点
- 给哪些参数
- 是否一起打开 RViz
- 是否加载模型

---

#### `spider_real.launch.py`
实机运行入口。

通常会启动：
- `robot_driver`
- `leg_calc`
- `spider_task`
- `robot_state_publisher`
- `rviz2`（可选）

这是你以后最常用的启动方式之一。

---

#### `spider_display.launch.py`
只做显示，不碰真实硬件。

这在学习阶段非常有用，因为：
- 没接舵机也能看模型
- 可以先验证 joint_state 和 URDF 是否正常
- 出问题时能分清是“显示层”还是“硬件层”

---

#### `spider_calibration.launch.py`
专门进入标定模式。

一旦你的工程变复杂，标定通常就不应该和正常运行混在一起。

单独 launch 的好处是：
- 目的明确
- 参数明确
- 不容易误启动危险动作

---

#### `spider_walk_test.launch.py`
专门做行走测试。

比如以后你想：
- 不开完整 UI
- 不开多余功能
- 只验证步态

那这种 launch 会非常方便。

---

#### `rviz/spider.rviz`
这是 RViz 的配置文件。

可以保存：
- 相机视角
- 显示哪些 topic
- 显示哪些坐标轴
- 显示哪些模型项

这样下次打开就不用重新调界面。

---

## 7. 节点之间到底怎么传数据

这是理解 ROS2 的关键部分。

推荐的数据流是：

```text
spider_task
   ↓
leg_calc
   ↓
robot_driver
   ↓
STM32 下位机
   ↓
18 路舵机
```

我们逐层看。

---

### 7.1 `spider_task`
它负责提出“意图”。

例如：
- 站立
- 趴下
- 前进
- 左转

它发出来的不是最终字节流，而是相对高层的命令。

---

### 7.2 `leg_calc`
它负责把意图变成具体角度。

例如：
- 根据前进速度算当前相位
- 根据相位算每条腿脚尖轨迹
- 根据脚尖位置算关节角
- 汇总成 18 路舵机角

---

### 7.3 `robot_driver`
它负责把 18 路角度发给 STM32。

例如：
- 把角度打包成固定长度字节帧
- 发到串口
- 等待下位机接收

---

### 7.4 STM32
STM32 再把命令真正变成硬件动作。

例如：
- 解析协议
- 控制舵机 PWM 或总线指令
- 执行动作

---

### 为什么这个链路很合理
因为每一层只解决一种问题：

- `task`：做什么
- `calc`：怎么算
- `driver`：怎么发
- `stm32`：怎么动

你以后遇到任何 bug，都可以沿着这条链去查。

---

## 8. Topic 是什么，为什么要这样命名

在 ROS2 里，topic 就像“广播频道”。

一个节点发布消息，另一个节点订阅消息。

例如：
- 某节点往 `/spider/servo_target` 发布舵机目标
- `robot_driver` 订阅这个 topic

---

### 推荐 topic 命名

#### 高层控制类
- `/spider/cmd_gait`
- `/spider/cmd_pose`
- `/spider/cmd_mode`

这类 topic 的特点是：
- 偏“我要做什么”
- 不是最终硬件量

---

#### 中间结果类
- `/spider/leg_target`
- `/spider/pose_target`
- `/spider/servo_target`

这类 topic 的特点是：
- 已经开始接近执行层
- 更偏中间结果或最终控制量

---

#### 驱动反馈类
- `/spider/state`
- `/joint_states`

这类 topic 的特点是：
- 用来告诉别人“我现在怎么样”
- 适合可视化、监控、调试

---

### 为什么 topic 要统一规范
因为当项目一大，命名混乱会非常痛苦。

比如如果你有：
- `/cmd`
- `/servo`
- `/state1`
- `/spider_state`
- `/driver_state`

别人一看根本不知道各自干嘛。

统一命名会让你的工程更专业，也更方便自己半年后回来看。

---

## 9. 为什么要强调“边界”

工程里最难的不是写出一个功能，而是：

> 写完十个功能后还不乱。

所以我们在文档里专门规定边界。

---

### 规则 1：驱动层不写步态
因为驱动层的工作就是通信。

如果把步态也塞进去，就会变成：
- 以后协议改了，步态代码也容易受影响
- 以后步态改了，驱动也容易被改坏

这叫**耦合过高**。

---

### 规则 2：任务层不碰串口细节
任务层只管“做什么”，不管：
- 帧头是什么
- 校验怎么算
- 串口设备名是什么

这样任务逻辑就能保持干净。

---

### 规则 3：参数不要到处写死
你以后一定会改：
- 舵机偏移
- 腿长
- 安装方向
- 默认姿态

如果这些写死在 cpp 文件里，以后会非常痛苦。

所以一开始就养成习惯：

> 能放配置文件的参数，尽量不写死在代码里。

---

### 规则 4：模块交流尽量走统一消息
这样好处是：
- 易调试
- 易打印
- 易替换实现
- 易接入其他节点

---

## 10. 第一阶段为什么只做 MVP

MVP 的意思是：

> **最小可行产品（Minimum Viable Product）**

简单说，就是：

- 先做最小闭环
- 不求一步到位
- 先保证系统真的跑起来

这是做工程非常重要的思想。

很多学生项目失败，不是因为能力不够，而是因为：

- 一开始目标太大
- 一上来就想做完整步态、完整 UI、完整仿真
- 最后哪部分都没真正跑通

所以我们要分阶段。

---

## 11. MVP 各阶段详细解释

### MVP-1：消息 + 驱动打通

这是最先要做的。

#### 为什么最先做它
因为如果连上位机到 STM32 都打不通，后面所有高级算法都没有意义。

#### 这一步你会学到什么
- ROS2 消息定义
- ROS2 发布/订阅
- 串口或 USB 通信
- 数据打包协议
- 上下位机联调

#### 完成标志
你在终端手动发一个 topic：
- `Servo18` 消息

然后 STM32 真能收到，舵机真能动。

这就是第一个重大里程碑。

---

### MVP-2：简单任务节点

#### 为什么第二步做它
因为你不能每次都手敲 topic 来测试。

所以要做一个简单任务节点，能自动发一些固定测试姿态。

#### 这一步你会学到什么
- ROS2 定时器
- 节点周期发布
- 舵机映射验证
- 动作测试流程

#### 建议最初动作
- 全部回中位
- 只抬左前腿
- 六条腿依次测试

这一步非常适合你学习，因为效果直观。

---

### MVP-3：加入 `leg_calc`

#### 为什么第三步再做它
因为逆运动学是比较容易出问题的部分。

如果你还没确认：
- 协议是通的
- 舵机顺序是对的
- 默认姿态是合理的

就直接上 IK，会让排错非常难。

#### 这一步你会学到什么
- 逆运动学
- 坐标系
- 几何关系
- 参数化设计

这是整个项目里最有“算法含量”的部分。

---

### MVP-4：补齐显示和启动

#### 为什么最后补
因为显示层虽然重要，但它不是最先决定机器人能不能动的关键。

等前面几层通了，再接 RViz 会更顺。

#### 这一步你会学到什么
- URDF / Xacro
- robot_state_publisher
- joint_states
- launch 文件组织
- 可视化调试

---

## 12. 对你来说，这个项目每一部分分别能学到什么

这一节我专门用“学习地图”的角度说。

### 做 `spider` 包，你会学到
- 机器人模型描述
- 参数配置组织
- URDF / Xacro 基础
- “机器人结构”和“机器人逻辑”要分开

### 做 `robot_interfaces` 包，你会学到
- 软件接口设计
- 数据结构抽象
- ROS2 消息系统
- 模块之间如何解耦

### 做 `robot_driver` 包，你会学到
- 串口通信
- 协议设计
- C++ 类拆分
- ROS2 节点和底层 IO 结合

### 做 `leg_calc` 包，你会学到
- 数学建模
- 逆运动学
- 坐标系和几何计算
- 参数和算法分离

### 做 `spider_task` 包，你会学到
- 状态机设计
- 高层控制逻辑
- 分阶段动作组织
- 任务调度思维

### 做 `launch_pack` 包，你会学到
- ROS2 系统组织方式
- 多节点协同启动
- 参数加载
- 调试流程标准化

也就是说，这个项目不是只会让你学“怎么控制一只蜘蛛”。

它还会让你学会：

- 如何组织一个真实机器人软件工程
- 如何把想法拆成模块
- 如何一步步排错
- 如何让代码从“能跑”变成“清楚、能维护”

---

## 13. 对一个大一结束学生的建议：怎么学这个项目最有效

我非常建议你不要只是“让我帮你生成一堆代码”。

最好的方式是：

### 方法 1：先懂结构，再写代码
每建一个包之前，先问自己三件事：

1. 这个包是干什么的？
2. 它不该干什么？
3. 它和其他包通过什么消息连接？

如果这三件事清楚了，你写代码会稳很多。

---

### 方法 2：每次只解决一层问题
例如：

- 这周只管 driver 打通
- 下周只管舵机测试姿态
- 再下一周只管单腿 IK

不要一口气全开。

---

### 方法 3：多画图
比如你可以经常手画：

- 数据流图
- 单腿关节示意图
- topic 流向图
- 状态机图

对机器人项目来说，图能极大帮助理解。

---

### 方法 4：每做完一小块就写总结
比如今天做完 `robot_driver`，你就写：

- 我今天解决了什么
- 遇到了什么问题
- 哪个参数最关键
- 这个模块和谁通信

这个习惯对成长特别有帮助。

---

## 14. 当前确定下来的工程骨架

本阶段最终先确定：

```bash
spider_upper/
└── src/
    ├── spider/              # 机器人模型、尺寸、零位、配置资料
    ├── robot_interfaces/    # 节点之间统一使用的消息接口
    ├── robot_driver/        # 上位机到 STM32 的协议桥和串口通信
    ├── leg_calc/            # 逆运动学、姿态补偿、角度解算
    ├── spider_task/         # 行为逻辑、状态机、测试动作、步态控制
    └── launch_pack/         # 一键启动不同运行模式
```

你可以把它再用一句最简单的话记住：

- `spider`：机器人资料库
- `robot_interfaces`：公共语言
- `robot_driver`：翻译官
- `leg_calc`：数学大脑
- `spider_task`：行为指挥中心
- `launch_pack`：启动面板

这六个比喻如果你记住了，整个项目就会清楚很多。

---

## 15. 下一步最合适做什么

既然你希望**边做边学**，那么我建议下一步不要直接糊一大堆代码，而是按教学式推进：

### 下一步推荐顺序
1. 按文档创建工作区和六个 package 空骨架
2. 我给你逐个解释每个 package 里的 `package.xml` 和 `CMakeLists.txt`
3. 先实现 `robot_interfaces` 的第一个消息 `Servo18.msg`
4. 再实现 `robot_driver` 的最小节点
5. 每做一步，我都给你解释“这一段代码为什么这样写”

这样你会真正学进去，而不是只得到一个“能跑但看不懂”的项目。

---

## 16. 结论

这份学习版文档最核心想告诉你三件事：

1. **这个项目不是一个文件，而是一套分层系统**
2. **每个 package 都有明确职责，不能乱串**
3. **你完全可以把它当成一个学习 ROS2 + 机器人软件工程的完整练习项目**

对你现在这个阶段来说，这个项目非常适合拿来练：

- 机器人软件结构
- ROS2 基础
- 串口协议
- 逆运动学
- 状态机
- 调试思维

如果你愿意，下一步我可以直接继续做两件事中的一个：

### 方案 A：继续完善文档
我再给你补一份：
- “每个 package 里未来每个文件大概写什么”
- 更像学习手册

### 方案 B：开始起骨架，但教学式进行
我一边创建目录和基础文件，一边给你解释：
- `package.xml` 是什么
- `CMakeLists.txt` 是什么
- 为什么 ROS2 包要这样写

如果你愿意，我建议下一句直接发：

**按教学方式起骨架，从 package.xml 开始讲。**
