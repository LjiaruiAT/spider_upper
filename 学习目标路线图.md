# Spider Upper 学习目标路线图

> 用途：把 `spider_upper` 接下来应该按什么顺序学、按什么顺序做，固定成一条清晰路线，避免“想到哪做到哪”。

---

## 当前总进度（粗略估计）

### 最终完整项目进度
- **约 30%**

这是按“完整蜘蛛机器人上位机”来估计的，包括：
- 配置层
- 高层控制接口
- 数学层
- 六足组织
- 步态
- 真发送
- 硬件联调

### 第一阶段软件骨架进度
- **约 75%**

这是按“当前阶段的软件骨架目标”来估计的，包括：
- 6 个 package 骨架
- `Servo18` 主链
- `robot_driver` 42 字节协议桥
- fake-send
- `leg_calc` 单腿 IK/FK 核心
- `Servo18Mapper`
- `servo_map.yaml` 接线
- `/spider/cmd_vel` 高层输入接口骨架

### 当前阶段定位
当前已经不在“刚入门”的阶段，而是进入：

> **从骨架阶段，进入接口接线阶段。**

---

## 总原则

### 1. 一次只推进一层
不要同时做很多主题。

例如不要一口气同时做：
- 遥控器接入
- 六足步态
- 真 USB 发送
- UI
- 标定

更合理的方式是：
- 这次只做配置层
- 下次只做高层接口
- 再下次只做六足静态组织

---

### 2. 每一阶段都要有验证标准
不是“写了很多代码”，而是要能回答：

> 这一阶段到底证明了什么？

例如：
- 证明配置真的驱动了代码
- 证明高层命令能进入系统
- 证明六条腿能统一输出 18 路数组

---

### 3. 优先做不会白做的部分
当前优先做：
- 配置层
- 接口层
- 数学层
- 主链层

后面再做：
- 真硬件发送
- 完整步态
- 遥控器真协议
- 标定和实机联调

---

## 当前所在阶段

当前工程已经完成了这些基础：
- 6 个 package 骨架
- `Servo18` 主链
- `robot_driver` 42 字节协议组帧
- fake-send 发送占位
- `leg_calc` 单腿 IK/FK 核心
- `Servo18Mapper`
- `servo_map.yaml` 已接入 mapper
- `/spider/cmd_vel` 高层输入接口骨架

所以当前已经不在“刚入门”的阶段，
而是进入：

> **从骨架阶段，进入接口接线阶段。**

---

# 阶段 1：彻底吃透当前骨架

## 当前估计进度
- **90%**

## 目标
不只是代码能跑，而是真正理解每个 package 的职责。

## 要理解的内容
- `spider`：配置和静态资料包
- `robot_interfaces`：公共消息定义
- `spider_task`：任务层
- `leg_calc`：数学层
- `robot_driver`：驱动层
- `launch_pack`：启动组织层

## 必须能回答的问题
- 谁发 `Servo18`？
- 谁收 `Servo18`？
- 谁负责 IK？
- 谁负责组帧？
- `servo_map.yaml` 归谁用？
- `/spider/cmd_vel` 现在是谁在收？

## 验证标准
能自己画出这条图：

```text
cmd_vel
-> spider_task
-> leg_calc
-> Servo18
-> robot_driver
```

---

# 阶段 2：把配置层真正接起来

## 当前估计进度
- **45%**

## 目标
让配置文件不再只是摆设，而是真正驱动代码。

## 推荐顺序
### 2.1 先做 `default_pose.yaml`
让 `spider_task` 从：
- `src/spider/config/default_pose.yaml`
读取默认姿态。

不要再把 18 路 `900` 写死在代码里。

### 2.2 继续完善 `servo_map.yaml`
当前已经接入 `Servo18Mapper`，后面要继续验证：
- 通道顺序是否合理
- 左右腿顺序是否合理
- 配置改动是否真的影响输出

### 2.3 后续再加
- `offset`
- `direction`
- 舵机修正参数

## 这一阶段主要学什么
- 为什么参数不能写死在 cpp 里
- 为什么配置层对机器人项目很重要
- 为什么“改配置”比“改代码”更适合调试和换硬件

## 验证标准
改一个 yaml，程序输出会跟着变。

---

# 阶段 3：把高层输入接口和任务层接稳

## 当前估计进度
- **60%**

## 目标
让 `/spider/cmd_vel` 不只是能收到，而是正式成为系统里的高层输入入口。

## 推荐顺序
### 3.1 理解 `/spider/cmd_vel`
当前先只使用：
- `linear.x`：前进 / 后退意图
- `linear.y`：左右平移意图
- `angular.z`：转向意图

### 3.2 让 `spider_task` 解释高层意图
例如：
- `vx = 0, vy = 0, wz = 0` -> 保持站立
- 有速度命令 -> 进入运动请求状态

### 3.3 暂时不要直接发舵机角
这一阶段重点不是 `cmd_vel -> Servo18`，
而是：

> 高层命令先进入系统，任务层先能理解它。

## 这一阶段主要学什么
- 高层意图 ≠ 舵机角
- `spider_task` 的职责是“决定做什么”
- `leg_calc` 的职责是“决定怎么算”

## 验证标准
发一条 `/spider/cmd_vel`，你能清楚看到：
- `spider_task` 收到它
- `spider_task` 缓存/打印它
- 原来的 `Servo18 -> robot_driver` 主链不被破坏

---

# 阶段 4：设计 `spider_task -> leg_calc` 的最小接口

## 当前估计进度
- **20%**

## 目标
把任务层和数学层真正接起来。

## 核心问题
`spider_task` 收到 `/spider/cmd_vel` 后，究竟把什么交给 `leg_calc`？

## 推荐思路
第一版先让 `leg_calc` 接收：
- `vx`
- `vy`
- `wz`

先不要一下子搞复杂自定义消息。

## 推荐顺序
### 4.1
先让 `leg_calc` 也能消费这类高层意图

### 4.2
先只做：
- 接收
- 缓存
- 打印

### 4.3
再往后才做：
- 由高层速度目标生成足端目标
- 由足端目标调用 IK

## 这一阶段主要学什么
- 系统分层接口应该怎么设计
- 为什么任务层和数学层不能混在一起
- 为什么先打通接口，再填计算逻辑更稳

## 验证标准
能明确说清：
- `spider_task` 输出什么
- `leg_calc` 输入什么
- 这两个层之间的职责边界是什么

---

# 阶段 5：从单腿数学走到六足静态组织

## 当前估计进度
- **25%**

## 目标
先做“六足统一解算”，不要急着做完整行走。

## 推荐顺序
### 5.1 六条腿的数据结构写全
例如：
- 六条腿枚举
- 六条腿默认中性点
- 六条腿统一目标容器

### 5.2 六条腿都能调用 IK
把 6 条腿的目标脚点都丢给 `leg_calc`。

### 5.3 统一进 `Servo18Mapper`
形成完整 18 路数组。

### 5.4 先做静态六足姿态验证
先不做步态，不做动态轨迹。

## 这一阶段主要学什么
- 单腿会算，不等于六腿会组织
- 六足数据结构和单腿数据结构的区别
- 为什么“静态六足”应该先于“动态六足”

## 验证标准
给六条腿默认目标点，能稳定输出一组完整的 18 路数组。

---

# 阶段 6：再进入步态 / vx vy 真正控制

## 当前估计进度
- **5%**

## 目标
让 `vx / vy / wz` 真正变成六足的运动输出。

## 要补的中间层
### 6.1 足端轨迹层
从 `vx / vy / wz` 生成随时间变化的脚尖目标。

### 6.2 相位层
决定：
- 哪条腿在支撑
- 哪条腿在摆动
- 相位差是多少

### 6.3 多腿协调层
让六条腿按统一节奏工作。

### 6.4 再调用 IK
最后得到 18 路关节目标。

## 这一阶段主要学什么
- IK 只是最后一层求解器，不是完整步态系统
- `vx / vy` 的真正难点在轨迹和相位，不在“会不会逆解”

## 验证标准
即使还没有真机，也至少能做到：
- 足端目标轨迹连续
- 逆解结果稳定
- 输出角度不是乱跳的

---

# 阶段 7：最后才做硬件联调

## 当前估计进度
- **5%**

## 目标
把软件链路真正接到下位机。

## 推荐顺序
### 7.1 把 fake-send 换成真发送
### 7.2 验证字节流能否发出
### 7.3 先单腿实机验证
### 7.4 再做整机联调

## 这一阶段主要学什么
- 软件正确 ≠ 硬件正确
- 单腿验证比整机联调更稳

## 验证标准
单腿一个简单动作能稳定执行，再扩大到整机。

---

# 完整工程任务拆分（按小任务）

> 说明：下面这些是按“尽量小、做完就能验证”的粒度拆出来的任务。每完成一个小任务，整体总进度就上涨一点。这里的“进度增加”指的是**最终完整项目进度**，按 100% 总盘子估算。

## A. 骨架与基础设施类（已完成为主）
- [x] 建立 `robot_interfaces` 包，并定义 `Servo18.msg`  **(+3%)**
- [x] 建立 `spider_task` 包和最小发布节点  **(+3%)**
- [x] 建立 `servo_listener_node` 学习型订阅节点  **(+2%)**
- [x] 建立 `robot_driver` 包和最小订阅节点  **(+3%)**
- [x] 实现 `Servo18 -> 42 字节协议帧` 组帧  **(+4%)**
- [x] 增加 fake-send 发送占位接口  **(+2%)**
- [x] 补齐 6 个目标 package 骨架  **(+4%)**
- [x] 增加 `launch_pack` 最小 launch 启动  **(+2%)**

## B. 配置层任务
- [x] 让 `spider_task` 从 `default_pose.yaml` 读取默认姿态  **(+4%)**
- [x] 去掉代码里写死的 18 路全 `900`  **(+2%)**
- [x] 让 `servo_map.yaml` 真正接入 `Servo18Mapper`  **(+3%)**
- [ ] 为 `Servo18Mapper` 增加 `offset` 修正支持  **(+3%)**
- [ ] 为 `Servo18Mapper` 增加 `direction` 修正支持  **(+3%)**
- [ ] 增加并接入 `servo_offset.yaml`  **(+2%)**

## C. 高层控制接口任务
- [x] 给 `spider_task` 增加 `/spider/cmd_vel` 输入接口  **(+3%)**
- [x] 让 `spider_task` 对 `cmd_vel=0` 和非 0 做最小状态区分  **(+2%)**
- [x] 定义 `spider_task -> leg_calc` 的最小输入接口  **(+3%)**
- [x] 让 `leg_calc` 也开始接收 `vx / vy / wz`  **(+3%)**
- [ ] 增加一个最小高层控制说明文档 / 接口注释  **(+1%)**

## D. 数学层任务
- [x] 提取单腿 IK/FK 数学核心 `LegKinematics`  **(+4%)**
- [x] 增加单腿公共数学类型 `common_types.hpp`  **(+1%)**
- [x] 增加 `Servo18Mapper` 软件桥骨架  **(+2%)**
- [ ] 让 `leg_calc` 读取真实腿参数而不是只用 demo chain  **(+4%)**
- [ ] 对齐蜘蛛单腿几何与 KDL 链结构  **(+4%)**
- [ ] 增加单腿目标点连续轨迹测试  **(+2%)**

## E. 六足静态组织任务
- [x] 定义六条腿默认脚点 / 中性点  **(+3%)**
- [x] 定义六足统一数据结构（六腿目标容器）  **(+2%)**
- [x] 让六条腿都能统一调用 IK  **(+4%)**
- [x] 六条腿统一生成 18 路数组  **(+3%)**
- [x] 验证静态六足默认姿态输出稳定  **(+2%)**

## F. 从高层命令到足端目标的过渡任务

- [x] 设计 `vx / vy / wz -> 足端目标` 的最小规则  **(+3%)**
- [ ] 先做最简单单腿 `vx` 方向轨迹实验  **(+2%)**
- [ ] 再做最简单单腿 `vy` 方向轨迹实验  **(+2%)**
- [ ] 把最小轨迹实验推广到一组腿  **(+3%)**

## G. 步态与相位任务
- [ ] 选择第一版六足步态策略（只定一种）  **(+2%)**
- [ ] 建立最小相位管理逻辑  **(+3%)**
- [ ] 建立支撑相 / 摆动相足端轨迹  **(+4%)**
- [ ] 完成最小 `vx` 行走演示  **(+4%)**
- [ ] 完成最小 `wz` 转向演示  **(+3%)**
- [ ] 再考虑 `vy` 平移演示  **(+2%)**

## H. 驱动与硬件联调任务
- [ ] 把 fake-send 换成真发送接口  **(+4%)**
- [ ] 打通 `/dev/ttyACM0` 或真实设备输出  **(+4%)**
- [ ] 验证下位机收包正确  **(+3%)**
- [ ] 单腿实机验证  **(+4%)**
- [ ] 六足整机实机验证  **(+4%)**

## I. 启动与调试体验任务
- [ ] 增加一个包含高层控制接口说明的 launch 模式  **(+1%)**
- [ ] 增加 `spider_display.launch.py` 或显示层启动骨架  **(+2%)**
- [ ] 增加 `spider_calibration.launch.py` 骨架  **(+1%)**
- [ ] 增加更适合后续调试的日志/状态输出  **(+1%)**

---

# 当前阶段待办清单（按优先级）

## P0：马上该做
- [ ] 让 `leg_calc` 真正发布 `Servo18`，而不是只打印结果
- [ ] 把 `spider_task -> leg_calc -> robot_driver` 接成正式主链
- [ ] 再检查 `servo_map.yaml` 和 `leg_params.yaml` 修改后是否能稳定影响输出

## P1：紧接着做
- [ ] 补一个最小高层控制说明文档 / 接口注释
- [ ] 明确 `vx=0, vy=0, wz=0` 时系统行为
- [ ] 明确非零速度输入时 `leg_calc` 当前静态组织输出如何变化

## P2：接口和数据流继续收紧
- [ ] 让 `leg_calc` 发布的结果真正替代 `spider_task` 当前默认姿态输出
- [ ] 不急着做完整步态，先把“高层意图 -> 六足静态输出”链路走通
- [ ] 继续清理 demo 逻辑和长期结构之间的边界

## P3：从静态组织过渡到简单轨迹
- [ ] 先做最简单单腿 `vx` 方向轨迹实验
- [ ] 再做最简单单腿 `vy` 方向轨迹实验
- [ ] 把最小轨迹实验推广到一组腿
- [ ] 验证输出是否连续稳定

## P4：后续再做
- [ ] 最简单的相位组织
- [ ] 最简单 `vx / vy` 动态演示
- [ ] 真发送与硬件联调

---

# 最近最优先的路线（短期任务）

如果只看你最近 1~2 周，最应该按这个顺序做：

## 1.
让 `leg_calc` 真正发布 `Servo18`

## 2.
把 `spider_task -> leg_calc -> robot_driver` 接成正式主链

## 3.
补一个最小高层控制说明文档 / 接口注释

## 4.
继续把 `vx / vy / wz` 对六足静态组织输出的影响讲清并验证清楚

## 5.
再开始尝试最简单的单腿 / 一组腿轨迹实验

---

# 当前最不该急着做的事

现阶段先不要把精力优先放在这些地方：
- 真 USB/CDC 发送
- 完整六足步态
- UI
- 太复杂的 FSM
- 遥控器真协议接入
- 力控 / VMC
- 很重的动力学

这些不是永远不做，而是：

> **现在做，收益没有前面几层高。**

---

# 每次开发时的固定节奏

以后每次推进，尽量按这个模板：

## 1. 本次只做一个主题
例如：
- 只做配置接线
- 只做高层接口
- 只做六足静态组织

## 2. 本次只回答一个问题
例如：
- 配置能不能驱动代码？
- 高层命令能不能进入系统？
- 六条腿能不能统一输出 18 路？

## 3. 做完就记录
每做完一小块，都写进：
- `LEARNING_LOG.md`
- `CURRENT_SUMMARY.md`

这样后面不会断上下文。

---

# 一句话总路线

> 先吃透骨架，再接配置层；先立高层接口，再接数学层；先做六足静态组织，再做动态步态；最后才做真实硬件联调。
