# Spider Upper 工程现状总结

> 用途：当前工程**唯一的事实来源**。记录"实际有什么、没有什么、哪些是假设"。
> 说明：本文件取代早期的 `CURRENT_SUMMARY.md`（后者已与代码不同步，仅作历史留档）。
> 更新时间：2026-09-16

---

## 1. 工程定位

`spider_upper` 是一个 **ROS2 Humble workspace**，目标是六足蜘蛛机器人的上位机。

当前阶段的原则：

> **先把不依赖硬件的软件写到位，再接真机。**

这意味着当前所有工作都应满足两个条件：
- 硬件无关，不会因为换舵机/换结构而白做
- 能通过日志或测试验证，不靠"感觉应该对"

**当前现实约束**
- 下位机协议已明确：固定 42 字节帧，18 路角度，单位 0.1°
- 上位机板（泰山派）采购
- 舵机型号未确认，机械结构未定，无实物尺寸
- **因此：IK 目前使用 demo KDL 链，不是真实腿几何**

---

## 2. 已确认的约定 / 假设 / 占位

这三类必须分清楚，否则会把"猜的东西"当成"对的东西"。

### 2.1 已确认（正式约定）

| 项 | 内容 | 位置 |
|---|---|---|
| 坐标系 | `spider_base`；`x` 前为正、`y` 左为正、`z` 下为负 | 代码 + 文档 |
| 单腿自由度 | 3 DoF，顺序恒为 `[coxa, femur, tibia]` | `common_types.hpp` |
| 关节角单位 | 弧度 rad（外部接口统一 rad） | `leg_calc` 全层 |
| 协议角度单位 | 0.1°（ddeg），90.0° 编码为 900 | `Servo18.msg` |
| 六腿索引 | `LF=0, LM=1, LR=2, RF=3, RM=4, RR=5` | `LegId` |
| 腿坐标系变换 | `p_body = body_T_leg · p_leg` | `common_types.hpp` |

### 2.2 待验证假设（接线后必须核对）

| 假设 | 说明 |
|---|---|
| 腿序 ↔ 舵机通道 | 目前按 `LF,LM,LR,RF,RM,RR` 顺序占 channel 0~17。真实接线大概率不同，**靠改 `servo_map.yaml` 修正，代码不用动** |
| 左右腿镜像 | 目前 6 条腿共用**同一条** KDL chain，无镜像。真实左右腿镜像安装后必然不同 |

### 2.3 占位值（不是设计值）

| 参数 | 当前值 | 说明 |
|---|---|---|
| `body_height_mm` | 250.0 | 为使默认站姿落进 demo 链可达空间而选，**非设计高度** |
| `coxa/femur/tibia_length_mm` | 0.0 | 占位，**代码根本没用它们** |
| demo KDL chain 的杆长 | `0.06 / 0.12 / -0.02 / 0.10 / -0.10` | 纯演示常量，与真实腿无关 |
| `default_left_y_mm` / `default_right_y_mm` | ±120.0 | 横向布局，未按真实结构核对 |
| `front/middle/rear_x_mm` | 180 / 0 / -180 | 中腿 x 曾误写为 120，已修正为 0 |

---

## 3. 已完成的软件链路（当前真实数据流）

```text
外部输入
  ↓  ros2 topic pub /spider/cmd_vel (geometry_msgs/Twist)
/spider/cmd_vel
  ↓  spider_task_node
      显式任务状态机：stand / move_request
      · 命令新鲜度判定（超时 0.25s）
      · 全零判定
      · 版本号变化才转发
      · 状态切换时主动发零
/spider/task_cmd_vel
  ↓  leg_calc_node（20ms 周期 timer）
      GaitPhaseManager.tick()
        ↓
      FootTrajectory（身体坐标系足端目标）
        ↓
      body_T_leg⁻¹
        ↓
      LegKinematics::inverse_position（KDL LMA 数值 IK）
        ↓
      forward_position（FK 回代，误差诊断）
        ↓
      Servo18Mapper::to_angle_ddeg
/spider/servo_target (robot_interfaces/Servo18)
  ↓  robot_driver_node
      pack_frame() → 42 字节协议帧
        ↓
      send_frame()  →  fake-send（仅打印）
```

旁路：`servo_listener_node` 订阅 `/spider/servo_target`，纯调试用，不属于主链。

### 42 字节协议帧

```text
Byte0    : 0xAA
Byte1    : 0x55
Byte2    : 36
Byte3    : SEQ
Byte4~39 : 18 路 int16 小端角度
Byte40   : XOR 校验
Byte41   : 0xBB
```

示例：`900 = 0x0384` 小端编码为 `84 03`。

---

## 4. 已完成（按包）

### 4.1 `robot_interfaces`
- `msg/Servo18.msg`：`std_msgs/Header header` + `int16[18] angle_ddeg` + `uint8 seq`

### 4.2 `spider`（资料/配置包）
- `config/leg_params.yaml`：机体高度、左右横向位置、前中后纵向位置
- `config/servo_map.yaml`：18 条 `channel → leg → joint` 映射
- `config/default_pose.yaml`：18 路 900（**目前无人读取**）
- `model/`：`sg90.stl` / `mg90s.stl` / servo_lib / OpenSCAD 生成脚本

### 4.3 `spider_task`
- `SpiderTaskContext`：命令缓存 + 时间戳 + 版本号 + 新鲜度
- `BaseTask` 抽象 + `on_enter()` / `process()`
- `StandTask` / `MoveRequestTask` 两个状态
- `SpiderTaskNode`：任务表 + 后台调度线程（50Hz）
- 订阅 `/spider/cmd_vel`，发布 `/spider/task_cmd_vel`
- `servo_listener_node` 调试订阅节点

### 4.4 `leg_calc`
- `common_types.hpp`：六腿枚举、六足容器、`LegMountPose`、body↔leg 变换
- `leg_kinematics`：KDL LMA 位置 IK（warm start）、FK、位置 Jacobian、速度 IK/FK
- `gait_types` / `gait_phase_manager`：`Tripod`（生效）、`Wave` / `Ripple`（已写未用）
- `foot_trajectory`：支撑相反向滑移、摆动相 `sin(πf)` 抬脚 + 前摆
- `servo18_mapper`：解析 `servo_map.yaml`，rad → ddeg，通道重排
- `main.cpp`：装配层，20ms 控制循环，读配置，**IK 回代误差诊断**
- **发布 `/spider/servo_target`（已接入主链）**

### 4.5 `robot_driver`
- 订阅 `/spider/servo_target`
- `pack_frame()`：`Servo18` → 42 字节
- `send_frame()`：fake-send（打印 HEX，不碰真实设备）
- `config/driver.yaml`（**目前无人读取**）

### 4.6 `launch_pack`
- `launch/spider_minimal.launch.py`：一键启动 `spider_task_node` + `leg_calc_node` + `robot_driver_node`

---

## 5. 本轮（2026-09-15 ~ 09-16）新增

| 项 | 内容 |
|---|---|
| 基线 | 固化了改动前的 git 基线，补齐了配置层接线现状 |
| 笔误修正 | `middle_x_mm: 120.0 → 0.0`（中腿应在身体正中间） |
| 默认站姿 | `body_height_mm: 120.0 → 250.0`（移入 demo 链可达空间） |
| 新增诊断 | `kIkErrorToleranceM = 1e-4`；逐腿统计 IK 返回码 + FK 回代误差，每周期汇总一条 `WARN`（1s 节流） |
| 重要结论 | demo 链可达空间是**半径 190.6 ~ 310.6 mm 的球壳**；120mm 的默认站姿**物理不可达** |

### 关键推导（值得记住）

demo 链后两段合并后长度为：

```text
|R2 · [0.22, 0, −0.12]| = √(0.22² + 0.12²) = 0.25060 m
```

加上第一段 0.06 m，可得可达半径区间：

```text
最近：0.25060 − 0.06 = 0.19060 m = 190.6 mm
最远：0.25060 + 0.06 = 0.31060 m = 310.6 mm
```

原目标 `(0,0,−0.12)` 距离 120 mm < 190.6 mm → 不可达。
求解器会收敛到**最接近**的位置（190.6 mm 处），误差 = 190.6 − 120 = **70.60 mm**（与日志完全一致）。
`ik = -101` = KDL `E_INCREMENT_JOINTS_TOO_SMALL`，即"撞到工作空间边界，再迭代也推不动"。

改到 250 mm 后，输出 `[0, 63, 688] × 6`，即 `coxa 0.0° / femur 6.3° / tibia 68.8°`，回代误差降到 0.1mm 量级。

---

## 6. 未完成（明确清单）

### 6.1 配置层（优先级最高）

- [ ] `default_pose.yaml` 接入（当前是孤儿文件，无人读取）
- [ ] `driver.yaml` 接入（当前是孤儿文件，无人读取）
- [x] `Servo18Mapper` 增加 `offset` 支持（字段并入 `servo_map.yaml`，见 D3）
- [x] `Servo18Mapper` 增加 `direction` 支持（左右镜像符号）
- [x] 标定字段并入 `servo_map.yaml`（不单独建 `servo_offset.yaml`）
- [ ] 标定数值本身（**必须实机标定**，当前全为 0 / +1）
- [ ] 下位机 `kServoMap` 的 offset/direction 确认为全 0 / +1（保持透传）
- [ ] `coxa/femur/tibia_length_mm` 真正用于构建 KDL chain
- [ ] 关节角限位（`q_min ≤ q ≤ q_max`）

### 6.2 数学层

- [ ] demo KDL chain 换成真实腿几何（**依赖实物尺寸**）
- [ ] 六腿左右镜像链（**依赖真实安装方式**）
- [ ] IK 失败处理：拒绝输出 / 保留上一帧 / 夹到工作空间内
- [ ] 正式可达性检查（IK 之前预判，而不是等 IK 失败）
- [ ] `apply_planar_turn()` 从一阶近似改成完整旋转
- [ ] 清理未使用的 `velocity_solver_`

### 6.3 步态 / 轨迹层

- [ ] `Wave` / `Ripple` 步态实际启用与验证
- [ ] 由 `vx/vy/wz` 生成连续足端轨迹（目前只做单周期插值，无跨周期连续）
- [ ] 步态周期与速度的物理解释（步长 × 频率 = 速度？）

### 6.4 任务层

- [ ] 补充状态：`walk` / `turn` / `stop` / `calibration`
- [ ] 起停的平滑过渡（避免姿态跳变）

### 6.5 消息层

目前只有 `Servo18`。设计文档中规划但**未实现**：

- [ ] `SpiderPose.msg`
- [ ] `SpiderState.msg`（含 `current_angle_ddeg`，是未来回读的落点）
- [ ] `GaitCommand.msg`
- [ ] `LegTarget.msg`

### 6.6 驱动层

- [ ] 真实串口 / USB CDC 发送（**依赖硬件**）
- [ ] `readback_enabled` 分支（回读解析，**依赖硬件**）
- [ ] 组帧与传输分层（`protocol` / `serial_port` 拆文件）
- [ ] 发布驱动状态
- [ ] `/joint_states`（供 RViz 使用）

### 6.7 验证与工具

- [ ] **无任何自动化测试**（最该补的一项）
- [ ] 无 URDF / Xacro 模型
- [ ] 无 RViz 配置（`launch_pack/rviz/README.md` 是空占位）
- [ ] launch 只有 `spider_minimal` 一种模式，缺 `display` / `calibration` / `walk_test` / `real`
- [ ] 无可视化（足端轨迹、关节曲线、相位）

### 6.8 硬件

- [ ] 上位机板（泰山派）
- [ ] 舵机型号确认与采购
- [ ] 单腿实机验证
- [ ] 整机联调
- [ ] 舵机零点标定

---

## 7. 已知问题清单

按"现在能不能修"分类。

### 纯软件可修（不依赖真机）

| # | 问题 | 影响 |
|---|---|---|
| 1 | IK 失败解仍被写入输出 | 上真机后可能发出危险角度 |
| 2 | 无关节限位 | 解出的角度可能超出舵机行程 |
| 3 | 6 腿共用一条 chain | 左右腿无法镜像 |
| 4 | `default_pose.yaml` / `driver.yaml` 是孤儿 | 配置层不完整，参数散落 |
| 5 | `offset` / `direction` 通路已建立，但**数值未标定** | 不标定时仍按透传输出，等于"数学零位 = 舵机零位"这个错误假设还在 |
| 6 | `publish_servo_target()` 读 `latest_task_cmd_vel_` 未加锁 | 轻微竞态 |
| 7 | 无自动化测试 | 改动容易静默破坏行为 |
| 8 | `leg_params.yaml` 的腿长参数为 0 且未使用 | 误导性配置 |

### 必须等真实结构

| # | 问题 | 等到什么时候 |
|---|---|---|
| 9 | demo chain ≠ 真实腿 | 拿到实物尺寸 |
| 10 | `body_height` 是凑出来的占位值 | 机械设计确定后 |
| 11 | 腿序 ↔ 通道映射是假设 | 接线后逐个测试舵机 |
| 12 | 无回读能力 | 硬件到位 |

### 可以放着不管（清理类）

| # | 问题 |
|---|---|
| 13 | `velocity_solver_` 声明未使用 |
| 14 | `kdl_parser` 依赖了但代码未 include |
| 15 | `apply_planar_turn()` 一阶近似 |

---

## 8. 常用命令

```bash
# 进入工作区
cd /home/liujiarui/Desktop/spider_upper

# 加载 ROS2
source /opt/ros/humble/setup.bash
export PYTHONPATH=/opt/ros/humble/lib/python3.10/site-packages:/opt/ros/humble/local/lib/python3.10/dist-packages:/usr/lib/python3/dist-packages

# 全量构建
colcon build --packages-select robot_interfaces spider robot_driver spider_task leg_calc launch_pack

# 只改了配置（src/spider/config/*.yaml）时
colcon build --packages-select spider
# 验证副本已更新：
grep body_height install/spider/share/spider/config/leg_params.yaml

# 加载本工程
source install/setup.bash

# 一键启动主链（3 个节点）
ros2 launch launch_pack spider_minimal.launch.py

# 单独跑数学层
ros2 run leg_calc leg_calc_node

# 单独跑 driver
ros2 run robot_driver robot_driver_node

# 发一条高层速度命令
ros2 topic pub /spider/cmd_vel geometry_msgs/msg/Twist \
  '{linear: {x: 0.2, y: 0.0, z: 0.0}, angular: {x: 0.0, y: 0.0, z: 0.3}}' -1

# 看话题
ros2 topic list
ros2 topic echo /spider/task_cmd_vel

# 看逐腿 IK/FK 细节（DEBUG 默认不输出，必须加 timeout 防刷屏）
timeout 3 ros2 run leg_calc leg_calc_node --ros-args --log-level leg_calc_node:=debug
```

**重要机制提醒**：`spider` 的配置在构建时被**复制**到 `install/spider/share/spider/config/`。
`leg_calc` 通过 `get_package_share_directory("spider")` 读的是 `install` 里那份。
所以**改 `src/spider/config/*.yaml` 后必须重新 build `spider` 才生效**，且**不要直接改 `install/` 里的文件**。

---

## 9. 进度评估

| 口径 | 进度 |
|---|---|
| 完整六足机器人上位机 | **约 30%** |
| 第一阶段"软件骨架"目标 | **约 80%** |
| 当前阶段定位 | 从"骨架阶段"进入"**接口接线 + 验证阶段**" |

---

## 10. 下一步候选（按推荐顺序）

### 推荐顺序

1. **文档同步**——把本文件之外的旧文档（`LEARNING_LOG.md` 顶部、`CURRENT_SUMMARY.md`）对齐到真实链路
2. **补自动化测试**——`FK(IK(p)) ≈ p`，覆盖可达与不可达两种用例。已有天然测试数据：
   - 不可达：`|p| = 120mm` → 误差 70.60mm
   - 可达：`|p| = 250mm` → 误差 < 0.1mm
3. **IK 失败处理**——拒绝 / 保留上一帧 / 夹取
4. **`offset` / `direction` 语义定义**（先定结构，不填值）
5. **步态层继续**——让 `vx/vy` 产生连续轨迹

### 现在不建议做

- 真 USB 发送（无硬件）
- 真实链 / 镜像链（无尺寸）
- 完整步态与动力学
- UI / 遥控器协议

原因：这些在当前阶段**收益低于上面几项**，且容易被后续结构变化推翻。

---

## 11. 下位机现状（`~/Desktop/spider/spder`）

> 该工程在工作区**之外**，属于下位机固件，不在本工程的 git 仓库里。

### 11.1 基本情况

| 项 | 内容 |
|---|---|
| 工程 | `spder`（Keil / MDK-ARM，`spder.ioc`） |
| 主控 | STM32F407VETx |
| 框架 | HAL + FreeRTOS + USB Device CDC |
| 调试 | J-Link 在线调试可用 |
| PWM | 18 路：TIM1_CH1~CH4、TIM2_CH1~CH4、TIM3_CH1~CH4、TIM4_CH1~CH4、TIM5_CH1~CH2 |
| PWM 参数 | 50Hz，周期 20000μs，计数 1000；500~2500μs 对应 0~180° |

### 11.2 用户代码位置

- `mytask/app.c` —— 用户核心任务
  - `get_data()`：接收任务，初始化 USB 接收层，把最新帧搬进共享缓存
  - `leg_cl()`：执行任务，周期把 18 路角度转成 PWM compare 并刷新
  - `ApplyServoCalibration()`：direction / offset 修正
  - `ApplyServoAngles()`：18 路统一刷新
  - `kBootPose[]`：开机姿态（当前全 900），保持 1 秒
  - `kServoMap[]`：18 路 → TIM 通道 + 标定参数
- `lib/usb_receiver.c/h`：42 字节协议解析
- `lib/usb_trans.c/h`：USB CDC 回调 → 任务上下文的数据转运
- `mytask/config.h`：**当前是空文件**（被 `app.h` include，疑似遗留或预留）

### 11.3 协议一致性（已验证对齐）

下位机 `lib/usb_receiver.h`：

```c
#define USB_SERVO_COUNT        18U
#define USB_FRAME_HEAD1        0xAAU
#define USB_FRAME_HEAD2        0x55U
#define USB_FRAME_PAYLOAD_LEN  36U
#define USB_FRAME_TOTAL_LEN    42U
#define USB_FRAME_TAIL         0xBBU
```

与上位机 `robot_driver::pack_frame()` **逐字节一致**，小端解析正确。这一层可以放心。

### 11.4 下位机已有的标定能力

```c
static int16_t ApplyServoCalibration(uint16_t index, int16_t angle_ddeg)
{
    int32_t corrected = angle_ddeg;
    if (kServoMap[index].direction < 0) {
        corrected = SERVO_MAX_DDEG - corrected;   /* 1800 - angle，按中位镜像 */
    }
    corrected += kServoMap[index].offset_ddeg;
    return ClampAngleDdeg(corrected);             /* 夹到 0~1800 */
}
```

顺序：**direction 镜像 → 加 offset → 夹到 0~1800**。

`1800 - angle` 是"以 900 为轴的镜像"，对 0~1800 行程的舵机来说是正确的反向写法。

**当前默认值：全部 `direction = 1`、`offset = 0`，等价于透传。**

### 11.5 下位机的其他行为

| 行为 | 实现 |
|---|---|
| 无数据保护 | 收到过至少一帧后才输出；之后没新帧就**保持最后一次有效角度** |
| 开机姿态 | `kBootPose[]` 全 900，保持 1 秒后再开始跟随上位机 |
| 角度夹取 | `ClampAngleDdeg` 强制 0~1800（硬保护） |
| **回读上报** | **没有**。下位机只收不发，`USB_DEVICE` 无状态上报通道 |

最后一条确认了一个重要判断：**上位机无法获得任何舵机反馈。**

---

## 12. 已决定的架构决策

| # | 决策 | 理由 |
|---|---|---|
| D1 | 坐标约定固定为 `spider_base`；`x` 前正 / `y` 左正 / `z` 下负 | 全局一致，不随实现变化 |
| D2 | 腿序 `LF,LM,LR,RF,RM,RR` ↔ channel 0~17 是**待验证假设** | 接线后靠改 `servo_map.yaml` 修正，代码不动 |
| D3 | **舵机标定参数（offset / direction）放在上位机** | 上位机有 YAML 配置系统、可版本管理、可离线校验；`/spider/servo_target` 语义因此明确为"真实舵机角（0~1800）" |
| D4 | 下位机 `ApplyServoCalibration` **保留**，仅作硬件微调预留，默认透传 | 只做兜底不做主标定，避免两边重复修正 |
| D5 | 下位机 `ClampAngleDdeg`（0~1800）**必须保留** | 上位机可能送出越界值，需要硬件侧硬保护 |
| D6 | J-Link 定位为**调试工具**，不作为标定参数的存储方式 | 标定结果需持久化、可版本管理；J-Link 在线改的值掉电即失 |
| D7 | `kServoMap` 是 `static const`（在 Flash），J-Link 不宜在线改 | 若要在线调参，需先把它改成 RAM 变量 |

### D3 的两种公式对照

**上位机（本次落地）**：

```text
servo_ddeg = direction × math_ddeg + offset_ddeg
```

语义可直接标定：
- `direction = ±1`：数学角增大时，舵机读数是增大还是减小
- `offset_ddeg`：数学角为 0 时，舵机应有的读数（单位 0.1°）

**下位机（保留为兜底层，默认透传）**：

```text
corrected = (direction < 0 ? 1800 − x : x) + offset_ddeg
```

两者形式不同，但**默认值下都是恒等变换**，不冲突。下位机只在需要现场微调硬件时启用。

### D3 的验证记录（2026-09-17）

用演示 offset 实测验证了"标定能消除越界"这个判断：

| 步骤 | 现象 |
|---|---|
| 原始状态（18 路 offset 全为 0） | 步态运行中每个周期出现 `标定后有 3/18 路角度超出 [0, 1800]，已夹取` |
| 只给 6 路 femur 加 `offset_ddeg: 900` | **WARN 归零**，18 路全部落在合法区间（femur 值域约 `659~973`） |
| 恢复为 0 | WARN 回来——这是**正确行为**，它如实反映"尚未实机标定" |

**结论**：越界不是几何错误，而是"数学零位 ≠ 舵机零位"的直接表现，标定参数就是它的正确归属位置。

> **注意**：演示用的 `900` 已改回 `0`。真实 offset 必须等舵机到位后实机测量，不要在配置里留一个"看起来合理"的假值。

---

## 13. 一句话总状态

> 六个 ROS2 package 的骨架、`Servo18` 主链、42 字节协议组帧、显式任务状态机、单腿 IK/FK 数学核心、六足静态组织、步态相位与足端轨迹层、配置驱动的默认布局，都已经落地并能编译运行；`cmd_vel → spider_task → leg_calc → servo_target → robot_driver` 全链路已打通（driver 端为 fake-send）。
>
> 但 IK 仍基于 demo 几何，映射层缺 `offset/direction`，六腿未镜像，无关节限位、无可达性预判、无自动化测试，配置层尚未收口，硬件联调未开始。
