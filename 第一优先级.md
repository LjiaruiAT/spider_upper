# 第一优先级：软件层第一阶段完成说明

> 目标：把六足机器人的软件链路做成“可运行、可解释、可扩展”的第一版，不接真实硬件。

---

## 1. 这一阶段实际做了什么

### 1.1 把 `spider_task` 从简单转发器改成显式任务状态机

#### 新增了什么
- `SpiderTaskContext`：统一保存最新命令、时间戳、新鲜度和转发版本
- `BaseTask`：统一任务接口，包含 `on_enter()` 和 `process()`
- `StandTask`：静止态
- `MoveRequestTask`：移动请求态
- `SpiderTaskNode`：任务表、任务切换、后台调度线程

#### 现在的效果
- 状态切换不再散落在回调里
- 命令过期会回到 `stand`
- 后面加 `walk`、`turn`、`stop` 时，不用重写整体结构

#### 文件位置
- `src/spider_task/src/main.cpp:31-105`：命令缓存上下文
- `src/spider_task/src/main.cpp:107-194`：任务基类、`stand`、`move_request`
- `src/spider_task/src/main.cpp:196-406`：任务节点、任务表、调度循环

---

### 1.2 把坐标系和腿的安装位姿显式化

#### 新增了什么
- `BodyFrame`：身体坐标系，默认 `spider_base`
- `BodyTwist`：身体速度输入
- `LegMountPose`：每条腿在身体坐标系里的安装位姿
- `SpiderFrameBundle`：把身体坐标系和六条腿的安装位姿组织起来
- `body_point_to_leg_point()` / `leg_point_to_body_point()`：显式坐标变换

#### 现在的效果
- 先在身体坐标系里生成足端目标
- 再转换到每条腿自己的局部坐标系
- 最后才做 IK

#### 文件位置
- `src/leg_calc/include/leg_calc/common_types.hpp:81-131`
- `src/leg_calc/src/main.cpp:186-236`
- `src/leg_calc/src/main.cpp:243-360`

---

### 1.3 把足端轨迹生成从主节点里拆出来

#### 新增了什么
- `FootTrajectory`
- `compute_foot_target()`
- `stance_trajectory()`
- `swing_trajectory()`
- `update_config()`

#### 现在的效果
- 轨迹层只处理身体坐标系下的足端目标
- `leg_calc` 负责坐标转换和 IK
- 以后调步态时，先改轨迹层，不用把整个节点一起动

#### 文件位置
- `src/leg_calc/include/leg_calc/foot_trajectory.hpp:13-48`
- `src/leg_calc/src/foot_trajectory.cpp:20-73`

---

### 1.4 把 gait phase manager 真正接进主链

#### 新增了什么
- `GaitPattern`、`GaitConfig`、`LegPhase`、`GaitState`
- `GaitPhaseManager`
- 三种相位更新：`Tripod` / `Wave` / `Ripple`

#### 现在的效果
- 步态相位不再只是“留着以后用”
- 主循环能按周期推进相位
- `FootTrajectory` 可以直接拿到相位状态来算轨迹

#### 文件位置
- `src/leg_calc/include/leg_calc/gait_types.hpp:7-57`
- `src/leg_calc/include/leg_calc/gait_phase_manager.hpp:7-33`
- `src/leg_calc/src/gait_phase_manager.cpp:9-131`

---

### 1.5 让 `leg_calc` 节点变成“装配层”而不是“大杂烩”

#### 新增了什么
- 周期控制 timer
- `control_loop()`：定期推进相位、生成轨迹、发布舵机目标
- `build_nominal_body_targets()`：静止站姿目标
- `build_motion_body_targets()`：运动时目标
- `solve_joint_targets()`：body frame → leg frame → IK → FK
- `publish_servo_target()`：发布 `Servo18`

#### 现在的效果
- `leg_calc/src/main.cpp` 更像节点装配层
- 轨迹、相位、IK、映射都各自有位置
- 静止态和运动态已经分开处理

#### 文件位置
- `src/leg_calc/src/main.cpp:128-182`：节点初始化、配置加载、timer 建立
- `src/leg_calc/src/main.cpp:186-236`：静止/运动目标生成与控制循环
- `src/leg_calc/src/main.cpp:243-306`：坐标转换、IK/FK、Servo18 发布

---

### 1.6 把数学层的编译结构补齐

#### 新增了什么
- 把 `foot_trajectory.cpp` 和 `gait_phase_manager.cpp` 纳入 `leg_calc_node` 构建目标

#### 现在的效果
- 数学层拆分后，工程还能正常编译
- 后面继续分层时，CMake 跟得上

#### 文件位置
- `src/leg_calc/CMakeLists.txt:16-58`

---

## 2. 这一步保留了什么

这次没有贸然引入外部 GitHub 运动学库，仍然保留：
- KDL
- Eigen

原因是：
- 当前最缺的是结构清晰，不是换一个库
- 先把 frame contract、任务状态机、轨迹层、步态层分开更重要
- 第三方库后续可以再评估，但不作为这一步前提

---

## 3. 验证结果

已经做过一次纯软件构建，结果通过：

- `robot_interfaces`
- `spider`
- `robot_driver`
- `spider_task`
- `leg_calc`
- `launch_pack`

说明第一优先级现在已经能编译通过。

---

## 4. 这一步之后的接口

现在已经铺好的后续入口是：
- 继续把 `FootTrajectory` 做完整
- 继续把 `GaitPhaseManager` 的策略扩展细化
- 再把 `leg_calc` 的 IK/FK 和轨迹层拆得更纯
- 以后让 `spider_task` 承接更完整的任务状态

---

## 5. 一句话总结

这一阶段已经把六足机器人的软件链路从“能转发”推进到“有明确坐标系、有任务状态机、有独立轨迹层、有步态相位管理、有清晰数学装配结构”的第一版。
