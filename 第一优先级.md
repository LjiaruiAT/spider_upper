# 第一优先级：软件层第一阶段完成说明

> 目标：先把六足机器人软件链路做成“可运行、可解释、可扩展”的第一版，不接真实硬件。

---

## 1. 这一步到底做了什么

### 1.1 把 `spider_task` 从简单转发器改成显式任务状态机

原来这里更像是一个“收到 `cmd_vel` 就直接转发”的节点。现在改成了更清楚的任务调度结构：

- `SpiderTaskContext` 统一保存最新命令、时间戳、新鲜度和转发版本
- `BaseTask` 作为任务接口，统一 `on_enter()` 和 `process()`
- `StandTask` 负责静止态
- `MoveRequestTask` 负责移动请求态
- `SpiderTaskNode` 负责任务表、任务切换、后台调度线程

这样做的好处是：
- 状态切换不再散落在回调里
- 以后加 `walk`、`turn`、`stop` 之类状态时，不用重写整体结构
- 任务层和后面的 `leg_calc`、`robot_driver` 更容易解耦

对应文件：
- `src/spider_task/src/main.cpp:31-105`：命令缓存上下文
- `src/spider_task/src/main.cpp:107-194`：任务基类、`stand`、`move_request`
- `src/spider_task/src/main.cpp:196-406`：任务节点和调度循环

---

### 1.2 把坐标系和腿的安装位姿显式化

这一步是为了把“默认理解”变成“代码里的明确约定”。

新增/强化了这些概念：
- `BodyFrame`：身体坐标系，默认 `spider_base`
- `BodyTwist`：身体速度输入
- `LegMountPose`：每条腿在身体坐标系里的安装位姿
- `SpiderFrameBundle`：把身体坐标系和六条腿的安装位姿组织起来
- `body_point_to_leg_point()` / `leg_point_to_body_point()`：显式坐标变换

现在的约定是：
- 先在身体坐标系里生成足端目标
- 再转换到每条腿自己的局部坐标系
- 最后才做 IK

对应文件：
- `src/leg_calc/include/leg_calc/common_types.hpp:81-131`：新增 frame 和变换相关类型
- `src/leg_calc/src/main.cpp:279-295`：构建六足安装位姿
- `src/leg_calc/src/main.cpp:171-210`：身体坐标系目标转腿坐标系目标，再做 IK

---

### 1.3 把足端轨迹生成从主节点里拆出来

以前 `leg_calc/src/main.cpp` 里同时做：
- 配置加载
- 足端目标生成
- IK/FK
- Servo18 映射
- 发布

现在第一步拆出一个独立的轨迹层：
- `FootTrajectory`
- `compute_foot_target()`
- `stance_trajectory()`
- `swing_trajectory()`
- `update_config()`

这样后面做步态时，就可以先改轨迹层，不用把整个节点逻辑一起动。

当前这层已经明确：
- 输入以身体坐标系为主
- 输出也先停留在身体坐标系
- 之后再交给 `leg_calc` 做坐标转换和 IK

对应文件：
- `src/leg_calc/include/leg_calc/foot_trajectory.hpp:13-48`
- `src/leg_calc/src/foot_trajectory.cpp:20-73`

---

### 1.4 让 `leg_calc` 节点变成“装配层”而不是“大杂烩”

`leg_calc/src/main.cpp` 现在的职责更清楚了：

- 从 `spider/config/leg_params.yaml` 读取布局参数
- 从 `spider/config/servo_map.yaml` 读取舵机映射
- 构建 `SpiderFrameBundle`
- 生成身体坐标系下的六足默认目标
- 转成腿坐标系目标
- 用 KDL 做 IK/FK
- 映射成 `Servo18`
- 发布 `/spider/servo_target`

也就是说，节点更像“把各个层拼起来”，而不是把所有算法都塞进一个文件里。

对应文件：
- `src/leg_calc/src/main.cpp:109-145`：节点初始化、配置加载、frame bundle 构建
- `src/leg_calc/src/main.cpp:148-210`：目标生成、坐标转换、IK/FK
- `src/leg_calc/src/main.cpp:212-241`：消息发布和日志输出

---

### 1.5 把数学层的编译结构补齐

因为新增了 `foot_trajectory.cpp`，所以 `leg_calc` 的 CMake 也补上了这个源文件。

对应文件：
- `src/leg_calc/CMakeLists.txt:16-40`

---

## 2. 这一步保留了什么

这次没有贸然引入外部 GitHub 运动学库，仍然保留：
- KDL
- Eigen

原因是：
- 现在最缺的是结构清晰，不是“再换一个库”
- 先把 frame contract、任务状态机、轨迹层分开更重要
- 第三方库后续可以再评估，但不作为这一步的前提

---

## 3. 验证结果

已经做过一次纯软件构建，结果通过：

- `robot_interfaces`
- `spider`
- `robot_driver`
- `spider_task`
- `leg_calc`
- `launch_pack`

说明这第一优先级的改动现在是能编译的。

---

## 4. 还没做完但已经铺好的后续入口

下一步最自然的继续方向是：
- 把 `gait_phase_manager` 真正接进运行链
- 用 `FootTrajectory` 生成步态轨迹，而不是只做静态偏移
- 继续把 `leg_calc` 的 IK/FK 和轨迹层拆得更纯
- 让 `spider_task` 后面承接更完整的任务状态

---

## 5. 一句话总结

这一阶段已经把六足机器人的软件链路从“能转发”推进到“有明确坐标系、有任务状态机、有独立轨迹层、有清晰数学装配结构”的第一版。
